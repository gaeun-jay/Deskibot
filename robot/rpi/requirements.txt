# picamera2 는 pip 으로 설치 불가 (libcamera 바인딩 필요) -> apt 로 먼저 설치할 것
#   sudo apt install -y python3-picamera2

Flask==3.1.3
mediapipe==0.10.18
numpy==1.26.4
opencv-contrib-python==4.11.0.86
pyserial==3.5
