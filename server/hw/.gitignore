# 자격증명 JSON. 이 디렉터리 바로 아래만 막는다 —
# bench/results/*.json 은 보고서 근거라 커밋 대상이다.
/*.json

# STT 벤치마크
# 녹음 원본(90개 wav)은 용량이 커서 제외한다. 반면 전사 결과·지표·그래프는
# 보고서의 근거이므로 커밋한다 — 이게 없으면 결론을 재검증할 수 없다.
bench/audio/
bench/audio_ulaw/
