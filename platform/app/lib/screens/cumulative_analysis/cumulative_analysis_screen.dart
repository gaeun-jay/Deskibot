import 'package:flutter/material.dart';

class CumulativeAnalysisScreen extends StatelessWidget {
  const CumulativeAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('누적'),
      ),
    );
  }
}
