#pragma once

// Deskibot device token은 시리얼 `token <값>` 명령으로 NVS에 저장한다.
// 컴파일 상수나 이 파일에는 평문 token을 넣지 않는다.

// Firestore (ToDo REST API가 준비될 때까지만 유지)
#define FIRESTORE_PROJECT_ID "your-project-id"
#define FIRESTORE_API_KEY    "your-firestore-api-key"
#define FS_USER_ID           "your-user-id"
